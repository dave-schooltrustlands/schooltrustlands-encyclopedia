// Site-wide configuration flags.
//
// PREPUBLICATION — when true, every Reading Room chapter page renders
// the <PrepublicationBanner /> warning at the top of the chapter content
// area. Phase 2 (publication-ready) is a one-line change: flip to false,
// rebuild, deploy. The banner disappears from every chapter page in one
// motion. Tracks the matching banner now carried at the top of the
// docx review files for both volumes.
export const PREPUBLICATION = true;
